syntax = "proto3";

package authorization;

option go_package = ".;pb";

import "process.proto";
import "reprocess.proto";

service AuthorizationService {
  rpc Process(AuthorizationProcessRequest) returns (AuthorizationProcessResponse);
  rpc Reprocess(AuthorizationReprocessRequest) returns (AuthorizationReprocessResponse);
}
